{
  "name": "recbot",
  "version": "1.0.0",
  "description": "recommendations queue bot for Discord",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "node --experimental-strip-types index.ts",
    "deploy-commands": "node --experimental-strip-types commands/deploy-commands.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@discordjs/builders": "^1.11.3",
    "@discordjs/rest": "^2.5.1",
    "discord-api-types": "^0.38.19",
    "discord.js": "^14.21.0",
    "dotenv": "^17.2.2",
    "node": "^22.20.0"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
